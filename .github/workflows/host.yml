name: Host CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: host-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  core:
    name: Core (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest

    steps:
      - name: Check out Wirelink
        uses: actions/checkout@v4

      - name: Configure
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release --parallel

  astrial:
    name: Astrial adapter (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest

    steps:
      - name: Check out Wirelink
        uses: actions/checkout@v4

      - name: Check out Astrial
        uses: actions/checkout@v4
        with:
          repository: starwey604/astrial
          path: astrial

      - name: Configure
        run: cmake -S . -B build -DWIRELINK_BUILD_ASTRIAL_ADAPTER=ON -DWIRELINK_ASTRIAL_SOURCE_DIR=${{ github.workspace }}/astrial -DBUILD_TESTING=ON -DBUILD_EXAMPLE=OFF -DASTRIAL_IO_URING=OFF -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release --parallel

      - name: Test
        run: ctest --test-dir build --build-config Release --output-on-failure

  sanitizers:
    name: Astrial adapter (ASan + UBSan)
    runs-on: ubuntu-latest

    steps:
      - name: Check out Wirelink
        uses: actions/checkout@v4

      - name: Check out Astrial
        uses: actions/checkout@v4
        with:
          repository: starwey604/astrial
          path: astrial

      - name: Configure
        run: >-
          cmake -S . -B build
          -DWIRELINK_BUILD_ASTRIAL_ADAPTER=ON
          -DWIRELINK_ASTRIAL_SOURCE_DIR=${{ github.workspace }}/astrial
          -DBUILD_TESTING=ON
          -DBUILD_EXAMPLE=OFF
          -DASTRIAL_IO_URING=OFF
          -DCMAKE_BUILD_TYPE=Debug
          -DCMAKE_C_FLAGS="-fsanitize=address,undefined -fno-omit-frame-pointer"
          -DCMAKE_CXX_FLAGS="-fsanitize=address,undefined -fno-omit-frame-pointer"
          -DCMAKE_EXE_LINKER_FLAGS="-fsanitize=address,undefined"

      - name: Build
        run: cmake --build build --parallel

      - name: Test
        env:
          ASAN_OPTIONS: detect_leaks=1:strict_string_checks=1
          UBSAN_OPTIONS: print_stacktrace=1:halt_on_error=1
        run: ctest --test-dir build --output-on-failure
