add_executable(wirelink_host_executor_tests executor.cpp)
target_link_libraries(wirelink_host_executor_tests PRIVATE Wirelink::host)
target_compile_features(wirelink_host_executor_tests PRIVATE cxx_std_20)
add_test(NAME wirelink_host_executor COMMAND wirelink_host_executor_tests)
add_executable(wirelink_coalescing_event_tests coalescing_event.cpp)
target_link_libraries(wirelink_coalescing_event_tests PRIVATE Wirelink::host)
add_test(NAME wirelink_coalescing_event COMMAND wirelink_coalescing_event_tests)
set_tests_properties(wirelink_host_executor wirelink_coalescing_event PROPERTIES TIMEOUT 30)
if(WIRELINK_HOST_PROFILING OR WIRELINK_HOST_LOCK_PROFILING)
  add_executable(wirelink_host_profile_tests profile.cpp)
  target_link_libraries(wirelink_host_profile_tests PRIVATE Wirelink::host)
  add_test(NAME wirelink_host_profile COMMAND wirelink_host_profile_tests)
  set_tests_properties(wirelink_host_profile PROPERTIES TIMEOUT 30)
endif()
